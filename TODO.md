# Positivus Full-Stack Live Setup - TODO

## Pending Steps:
1. ✅ **Plan approved by user**
2. ✅ Create `.env.local` (frontend)
3. ✅ Create `backend/.env` (backend with JWT secrets, SMTP optional)
4. ❌ Docker not installed - skipping DB/API Docker, using local dev instead
5. Backend: cd backend && npm run db:migrate db:generate (needs local Postgres)
6. ✅ Frontend `npm install` completed
7. ✅ Backend `npm install` running (in backend dir)
8. ✅ Frontend `npm run dev` running on http://localhost:5174 (port 5173 busy, auto-changed)
9. Start backend dev: New terminal → cd backend && npm run dev (needs Postgres)
10. Install Docker Desktop for full prod (DB + API Docker)

## Completed:
- Env files created

**Next: Step 4 (Docker up)**
